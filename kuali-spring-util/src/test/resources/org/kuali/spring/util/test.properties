# The application is only aware of properties that start with 'cat'
cat=lion
cat.speed=${${cat}.speed}
cat.actions=${${cat}.actions}
cat.prey=${${cat}.prey}

# Lion specific properties
lion.speed=average
lion.prey=zebra
lion.actions=Have the lioness hunt a ${${cat}.prey} and bring the ${${cat}.prey} back to the pride  

# Cheetah specific properties
cheetah.speed=fast
cheetah.prey=gazelle
cheetah.actions=Chase a ${${cat}.prey} and drag the ${${cat}.prey} high into a tree

# Test that placeholders in keys get resolved correctly
cat.${cat}.speed=${${cat}.speed}

my.password=it is a secret

plogger.lf=LF
plogger.cr=CR


