<config>

    <!-- Standalone Server Data Source  -->
    <param name="datasource.ojb.platform">Oracle9i</param>
    <param name="datasource.platform">org.kuali.rice.core.database.platform.OraclePlatform</param>
    <param name="datasource.url">jdbc:oracle:thin:@dbsampleu.edu</param>
    <param name="datasource.username">my_rice_schema</param>
    <param name="datasource.password">my_rice_password</param>
    <param name="datasource.driver.name">oracle.jdbc.OracleDriver</param>
    <param name="datasource.pool.minSize">1</param>
    <param name="datasource.pool.maxSize">10</param>
    <param name="datasource.pool.maxWait">5000</param>
    <param name="datasource.pool.lifeTime">5000</param>
    <param name="datasource.pool.sleepTime">5000</param>
    <param name="datasource.pool.validationQuery">select 1 from dual</param>
    <param name="datasource.pool.inactiveConnectionTimeout">120</param>
    <param name="datasource.pool.maxStatements">0</param>

    <!-- KSB -->
    <!--  param name="serviceServletUrl">http://${machine.url}/${nonroot.context}/remoting/</param>
    <param name="keystore.file">${rice.config.path}/rice-dev.keystore</param>
    <param name="keystore.alias">rice-dev</param>
    <param name="keystore.password">rice-dev</param>
    <param name="threadPool.size">20</param -->

    <!-- KEN -->
    <param name="notification.basewebappurl">${application.url}/ken</param>

    <!-- KEW -->
    <param name="workflow.url">${application.url}/kew</param>
    <param name="attachment.dir.location">${rice.config.path}/kew_attachments</param>
    <param name="userOptions.default.email">no</param>


    <!-- External Document Handlers -->
    <param name="external.handler.url.1"></param>
    
</config>