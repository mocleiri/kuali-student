repository tools

require-jira-pre-commit-hook.sh

This should be renamed to $REPO/hooks/pre-commit and made executeable.

setup-and-test-jira-pre-commit-hook.sh

This will create a testing repository and apply some commits to make sure that the pre-commit hook is working properly.
